server:
    port: 1200
    servlet:
        context-path: /tasty-town

spring:
    application:
        name: backend
    datasource:
        url: jdbc:postgresql://localhost:5432/tasty_town
        username: postgres
        password: root
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
